# Mac 本地开发环境变量（172.20.10.8）
# 只覆盖"对外暴露给设备/Pilot App"的地址；其余变量沿用 docker-compose.yml 内默认值。
# Nano 节点部署请用 .env.nano1（配合 docker-compose.nano.yml），不要把两个混用。

# MQTT Broker 对外地址：登录接口返回给 Pilot App / 遥控器的 mqtt_addr 由它决定，
# 必须是局域网设备能访问的 Mac IP（app 容器自己连 mqtt 用的是内部服务名，互不影响）
MQTT_HOST=172.20.10.8

# MinIO 对外地址：航线/媒体文件预签名 URL 由它生成，设备需要能访问
#（app 容器自己访问 minio 用的是内部地址 http://minio:9000，互不影响）
OSS_EXTERNAL_ENDPOINT=http://172.20.10.8:9100
